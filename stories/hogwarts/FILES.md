# stories/hogwarts/FILES.md — Local Structure

Defers to master `FILES.md` for storage/hosting/secrets.

## Files this story owns
- `pages.json` — panel data (TO BE WRITTEN from the poem's beats).
- `index.html` — assembled comic page (later).
- Cover image — generated later; referenced from `stories.json` (`cover`).

## Boundaries
- Uses the shared cast in `/characters`. New magical-friend characters: if recurring,
  promote to `/characters`; if one-off, keep local. See the IP note in SPEC.
