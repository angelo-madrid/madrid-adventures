# stories/mars/FILES.md — Local Structure

Defers to master `FILES.md` for storage/hosting/secrets.

## Files this story owns (to be created)
- `pages.json` — panel data (after the story is written).
- `index.html` — assembled comic page (later).
- Cover image — generated later; referenced from `stories.json` (`cover`).

## Boundaries
- Uses the shared cast in `/characters`.
