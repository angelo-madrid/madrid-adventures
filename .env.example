# Copy to .env (which is git-ignored) and fill in. Never commit the real .env.
# The published site uses ONLY the anon/public key. The service key is for local
# build/upload scripts only.
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_KEY=
